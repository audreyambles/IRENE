using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class casUsage : MonoBehaviour
{
    public UsagerData usager1;
    //délais entre les stimuli
    public int delais = 30;

    // gestion des règles
    // en fonction du "niveau attendu de l'expérience MS" ( 0 = facile (enfants), 1 = moyen (de l'aide, on veut quand même trouver vite), 2 = dur (les stimuli les plus durs en premier, on veut faire réfléchir le spectateur))
    // if CS = 2 stim en même temps
    // if SurchS = pas deux fois de suite le meme stim
    // if DS = le sens prio en premier (signature malvoyant)
    // if CCM = ?


       // gestion des règles ici, sous forme de fonctions ? ou liste de règles ?
}
